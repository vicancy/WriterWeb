input(type='hidden', value=selectedArticleId)#initSelectedArticleId
ul
  each item in items
    li
      a(href='javascript:void(0)', onclick='loadArticle(#{item._id})') #{item.Title}
      p #{item.Abstract}
      hr